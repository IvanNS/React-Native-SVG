import React from 'react'

const MenuBar = props => {
    return ""
}

const styles = StyleSheet.create({
    screen:{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    }
});

export default MenuBar;